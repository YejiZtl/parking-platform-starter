# 停车场识别规则与参数说明

本文档只做规则索引，避免和代码维护两套参数。当前运行默认值以 `parking_config.py` 的 `DEFAULTS` 为准；部署环境可用 `.env` 和命令行覆盖。

## 处理流程

```text
读取视频帧
  -> 周期性 YOLO 车辆检测
  -> 停车场 ROI 过滤
  -> 同车重复检测框过滤
  -> 车辆框与车位区域匹配
  -> 占用状态防抖
  -> 输出标注画面、JSONL 统计、运行日志和健康状态
```

## 规则源文件

- 检测参数默认值：`parking_config.py`
- ROI、车位缩放和重复框几何：`parking_geometry.py`
- 车位占用、黄色疑似、一车一位、防抖：`managed_parking.py`
- 运行日志、JSONL 轮转、健康状态、MP4 会话文件：`run_parking.py`
- 校准文件：`parking_calibration.json`
- 维护流程：`docs/MAINTENANCE.md`

## 关键语义

当前系统不是多目标跟踪。`run_parking.py` 调用的是 `model.predict`，检测结果按当前帧参与 ROI 过滤和车位匹配；跨帧稳定性来自车位状态防抖，不来自 tracker。

车位来自 `bounding_boxes.json`，ROI 来自 `parking_roi.json`。两者与参考帧尺寸、哈希和期望车位数量的关系由 `parking_calibration.json` 校验。默认情况下，摄像头分辨率变化会中止启动，防止静默错位。

重复框过滤和 ROI 内外判定由 `parking_geometry.py` 统一提供，生产运行、自动标注、标签审计和评估脚本复用同一实现。

视频输出边界：本地代码只写 OpenCV `mp4v` MP4 文件。NVENC、MediaMTX、HLS 和浏览器播放地址需要由外部部署链路提供，不能从本地代码推断。

## 调参入口

先改 `.env` 或命令行覆盖；只有需要改变项目默认基线时才改 `parking_config.py`。修改后运行：

```powershell
python -m unittest discover -s tests -v
```

涉及 ROI、车位或参考帧时，还要运行：

```powershell
python update_calibration.py --image first_frame.jpg --regions bounding_boxes.json --roi parking_roi.json
```

服务器排查时必须在服务器项目目录查看真实 `.env`、进程命令和加载模型路径。本地 `.env` 不能代表服务器。
